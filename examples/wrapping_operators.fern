fn main() -> void {
    var wrapped: u8 = 250 +% u8(10);
    var zero: u8 = u8(1) +% -%u8(1);
    var scaled = 1 + 1 << 5;
    var cleared: u8 = u8(0x0F) & ^u8(0x0A);
    exit(int(wrapped) + int(zero) + scaled + int(cleared));
}
